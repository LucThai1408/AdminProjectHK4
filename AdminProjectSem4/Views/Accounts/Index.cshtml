@using Microsoft.AspNetCore.Mvc.Rendering
@model IEnumerable<API.Models.Account>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var currentPage = ViewBag.CurrentPage;
    var totalPage = ViewBag.TotalPage;
    var name = ViewBag.Name as string;
    var userRole = User.FindFirst(System.Security.Claims.ClaimTypes.Role)?.Value;
}
<div class="container"> 
<h1>Quản lý tài khoản</h1>
<div class="btn-success text-center text-uppercase">@TempData["msg"]</div>

@if (userRole == "1")
{
    <p>
        <a asp-action="Create" asp-controller="Accounts" class="btn btn-primary">Create New</a>
    </p>
}

    <form method="get" class="d-flex align-items-center gap-3 mb-3 flex-wrap">
        <!-- Ô tìm kiếm theo tên -->
        <div class="flex-grow-1 rounded-0 mr-3" style="min-width: 200px">
            <input type="text" name="name" class="form-control"
                   placeholder="Tìm kiếm theo tên, email, fullname"
                   value="@ViewBag.Name" />
        </div>

        <!-- Dropdown Role -->
        <div>
            <select name="role" asp-items="@(ViewBag.RoleList as SelectList)"
                    class="form-select p-2 mx-3" style="min-width: 150px;">
            </select>
        </div>

        <!-- Dropdown Status -->
        <div>
            <select name="status" asp-items="@(ViewBag.StatusList as SelectList)"
                    class="form-select p-2 mx-3" style="min-width: 150px;">
            </select>
        </div>

        <!-- Nút lọc -->
        <div>
            <button type="submit" class="btn btn-primary px-4 rounded-0 p-2">Lọc</button>
        </div>
    </form>


    <table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>AccountId</th>
            <th>UserName</th>
            <th>FullName</th>
            <th>Email</th>
            <th>Role</th>
            <th>Status</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.AccountId)</td>
                <td>@Html.DisplayFor(modelItem => item.Name)</td>
                <td>@Html.DisplayFor(modelItem => item.FullName)</td>
                <td>@Html.DisplayFor(modelItem => item.Email)</td>

                @if (item.Role == 0)
                {
                    <td>Sinh viên</td>
                }
                else if (item.Role == 1)
                {
                    <td>Admin</td>
                }
                else if (item.Role == 2)
                {
                    <td>Giảng viên</td>
                }
                else
                {
                    <td>Không xác định</td>
                }

                    <td>@(item.Status ? "Active" : "InActive")</td>

                <td>
                    @if (userRole == "1")
                    {
                        <a asp-action="Edit" asp-route-id="@item.AccountId" class="btn btn-success">Edit</a>
                        <a asp-action="Details" asp-route-id="@item.AccountId" class="btn btn-info">Details</a>

                        @if (item.Status)
                        {
                            <!-- ✅ Khi đang hoạt động -->
                            <form asp-action="Deactive" asp-controller="Accounts" method="post" style="display:inline;">
                                <input type="hidden" name="id" value="@item.AccountId" />
                                <button type="submit" class="btn btn-danger"
                                        onclick="return confirm('Do you sure wanna deactive this?')">
                                    Deactive
                                </button>
                            </form>
                        }
                        else
                        {
                            <!-- ⚠️ Khi đã bị vô hiệu hóa -->
                            <button class="btn btn-secondary" disabled title="This account is already deactivated">
                                Deactivated
                            </button>
                        }
                    }
                    else if (userRole == "2")
                    {
                        <a asp-action="Details" asp-route-id="@item.AccountId" class="btn btn-info">Details</a>
                    }
                </td>

            </tr>
        }
    </tbody>
</table>

@if (Model.Count() != 0)
{
    @if (string.IsNullOrWhiteSpace(name))
    {
        <nav aria-label="Page navigation">
            <ul class="pagination justify-content-center">
                <li class="page-item @(currentPage == 1 ? "disabled" : "")">
                    <a class="page-link" href="?currentPage=@(currentPage - 1)" aria-label="Previous">
                        <span aria-hidden="true">&laquo;</span>
                    </a>
                </li>
                @for (int i = 1; i <= totalPage; i++)
                {
                    <li class="page-item @(i == currentPage ? "active" : "")">
                        <a class="page-link" href="?currentPage=@i">@i</a>
                    </li>
                }
                <li class="page-item @(currentPage == totalPage ? "disabled" : "")">
                    <a class="page-link" href="?currentPage=@(currentPage + 1)" aria-label="Next">
                        <span aria-hidden="true">&raquo;</span>
                    </a>
                </li>
            </ul>
        </nav>
    }
    else
    {
        <nav aria-label="Page navigation">
            <ul class="pagination justify-content-center">
                <li class="page-item @(currentPage == 1 ? "disabled" : "")">
                    <a class="page-link" href="?currentPage=@(currentPage - 1)&name=@name" aria-label="Previous">
                        <span aria-hidden="true">&laquo;</span>
                    </a>
                </li>
                @for (int i = 1; i <= totalPage; i++)
                {
                    <li class="page-item @(i == currentPage ? "active" : "")">
                        <a class="page-link" href="?currentPage=@i&name=@name">@i</a>
                    </li>
                }
                <li class="page-item @(currentPage == totalPage ? "disabled" : "")">
                    <a class="page-link" href="?currentPage=@(currentPage + 1)&name=@name" aria-label="Next">
                        <span aria-hidden="true">&raquo;</span>
                    </a>
                </li>
            </ul>
        </nav>
    }
}
else
{
    <p class="text-center">No item!</p>
}
</div>