@model API.Models.Subject

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Update a subject</h1>
<div class="container">

    <div class="btn-danger text-center">@ViewBag.Error</div>
    <hr />
    <section class="content mt-4">
        <div class="container-fluid">
            <div class="row justify-content-center">
                <div class="col-md-6">
                    <form asp-action="Edit" method="post">
                        <input asp-for="SubjectId" hidden class="form-control" />
                        <div class="form-group mb-3">
                            <label asp-for="Name" class="form-label fw-bold">Subject Name</label>
                            <input asp-for="Name" class="form-control" placeholder="Enter subject name..." />
                            <span asp-validation-for="Name" class="text-danger small"></span>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="MaxScore" class="form-label fw-bold">Max Score</label>
                                <input asp-for="MaxScore" type="number" class="form-control" placeholder="e.g. 100" />
                                <span asp-validation-for="MaxScore" class="text-danger small"></span>
                            </div>

                            <div class="col-md-6 mb-3">
                                <label asp-for="PassScore" class="form-label fw-bold">Pass Score</label>
                                <input asp-for="PassScore" type="number" class="form-control" placeholder="e.g. 50" />
                                <span asp-validation-for="PassScore" class="text-danger small"></span>
                            </div>
                        </div>

                      @*   <div class="form-check mb-3">
                            <input asp-for="Status" class="form-check-input" checked />
                            <label asp-for="Status" class="form-check-label">Active</label>
                        </div> *@

                        <div class="d-flex justify-content-end">
                            <button type="submit" class="btn btn-success me-2 mr-3">Save</button>
                            <a asp-action="Index" class="btn btn-primary">Back to List</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </section>

    @section Scripts {
        @{
            await Html.RenderPartialAsync("_ValidationScriptsPartial");
        }
    }

</div>